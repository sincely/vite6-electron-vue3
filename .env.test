VITE_ENV = 'test'
VITE_BASE_URL = './' # 打包后资源路径
VITE_USE_PROXY = true #是否使用DevServer代理
VITE_USE_LEGACY = true #是否使用`legacy`插件，打包后会生成兼容旧浏览器的代码，增加包体积和构建时间
VITE_API_BASE_URL = 'http://10.10.24.52:8089' # API接口基础URL
VITE_APP_NAME = 'Crab' # 应用名称
